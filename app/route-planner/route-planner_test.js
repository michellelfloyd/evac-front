'use strict';

describe('myApp.route-planner module', function() {

  beforeEach(module('myApp.route-planner'));

  describe('route-planner controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RoutePlannerCtrl = $controller('RoutePlannerCtrl');
      expect(RoutePlannerCtrl).toBeDefined();
    }));

  });
});